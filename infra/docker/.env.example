# ══════════════════════════════════════════════════════════
# Muraho Rwanda — Environment Configuration
# Copy to .env and fill in production values
# ══════════════════════════════════════════════════════════

# ── PostgreSQL ────────────────────────────────────────────
POSTGRES_USER=muraho
POSTGRES_PASSWORD=muraho_secret_CHANGE_ME
POSTGRES_PORT=5432

# ── Payload CMS ───────────────────────────────────────────
PAYLOAD_SECRET=muraho-payload-secret-CHANGE-ME-64-chars-minimum
PAYLOAD_PORT=3000

# ── Redis ─────────────────────────────────────────────────
REDIS_PORT=6379

# ── MinIO (S3-compatible storage) ─────────────────────────
MINIO_ROOT_USER=muraho_minio
MINIO_ROOT_PASSWORD=muraho_minio_secret_CHANGE_ME
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# ── AI Service ────────────────────────────────────────────
AI_PORT=8000
OLLAMA_PORT=11434

# ── Stripe (International payments — USD/EUR/GBP) ────────
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# ── Flutterwave (Local payments — MTN MoMo, Airtel) ──────
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-...
FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST-...
FLUTTERWAVE_WEBHOOK_HASH=...

# ── ElevenLabs TTS (optional — falls back to self-hosted) ─
ELEVENLABS_API_KEY=...

# ── Frontend ──────────────────────────────────────────────
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3000/api

# ── Production (--profile production) ─────────────────────
# APP_URL=https://muraho.rw
# MAPBOX_TOKEN=pk.eyJ...
